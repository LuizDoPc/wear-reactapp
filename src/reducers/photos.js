import Actions from "../actions";

const defaultState = {
  photos: []
};

export default function Reducer(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
